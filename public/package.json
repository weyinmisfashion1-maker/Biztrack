{
  "name": "biztrack-ng",
  "version": "1.0.0",
  "description": "Daily sales, expenses, and inventory manager for Nigerian SME owners.",
  "main": "app.js",
  "scripts": {
    "start": "powershell.exe -File server.ps1",
    "build": "copy index.html www\\index.html && copy login.html www\\login.html && copy app.js www\\app.js && copy reset-password.html www\\reset-password.html && copy onboarding.html www\\onboarding.html && npx cap copy",
    "open-android": "npx.cmd cap open android"
  },
  "dependencies": {
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
